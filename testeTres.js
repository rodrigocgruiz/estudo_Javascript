let contador = 0;
let pedido = 100;

//pedido = parseInt(prompt("Digite um número: "));
//contador = pedido;

while (contador <= pedido) {
    console.log('Contagem progressiva: ' + contador);
    contador++;
}